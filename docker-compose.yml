services:
  backend:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./src:/app/src
      - ./data:/app/data
      - ./logs:/app/logs
      - ./frontend:/app/frontend
      - ./tests:/app/tests
    environment:
      - PYTHONUNBUFFERED=1
